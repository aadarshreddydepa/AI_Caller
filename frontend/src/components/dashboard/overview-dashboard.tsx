"use client";
import { CalendarDays, ChevronDown, PhoneCall, Sparkles, UsersRound } from "lucide-react";
import Link from "next/link";
import { useEffect, useState } from "react";
import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { displayName } from "@/lib/format";
import type { DashboardData, Session } from "@/lib/types";

export function OverviewDashboard({ session, data }:{ session:Session; data:DashboardData }) {
  const [referenceTime] = useState(() => Date.now());
  const name = displayName(session.user.name, session.user.email);
  const callbackCount = Math.max(data.leads.total - data.calls.escalated, 0);
  const resolved = Math.max(data.calls.total - callbackCount - data.calls.escalated, 0);
  const resolvedRate = data.calls.total ? Math.round(resolved / data.calls.total * 100) : 0;
  const activity = data.activity.map(item => ({ day:new Intl.DateTimeFormat("en", { weekday:"short" }).format(new Date(`${item.date}T12:00:00`)), calls:item.count }));
  const outcomes = [{ name:"Resolved", value:resolved, color:"#6059ef" }, { name:"Callback", value:callbackCount, color:"#f5b650" }, { name:"Escalated", value:data.calls.escalated, color:"#f2747c" }].filter(item => item.value > 0);
  useEffect(() => {
    const context = document.modelContext; if (!context?.registerTool) return; const lifecycle = new AbortController();
    void Promise.resolve(context.registerTool({ name:"read_call_dashboard_summary", title:"Read call dashboard summary", description:"Read the authenticated business call and lead summary.", inputSchema:{ type:"object", properties:{}, additionalProperties:false }, annotations:{ readOnlyHint:true, untrustedContentHint:false }, execute:async () => ({ business:data.business.name, calls:data.calls, leads:data.leads, activity:data.activity }) }, { signal:lifecycle.signal })).catch(() => undefined);
    return () => lifecycle.abort();
  }, [data]);
  return <div className="content"><section className="page-heading"><div><p>{new Intl.DateTimeFormat("en-IN", { weekday:"long", day:"numeric", month:"long" }).format(new Date())}</p><h1>Good evening, {name}</h1><span>Here’s how your AI receptionist is performing today.</span></div><button className="primary-button"><PhoneCall size={17}/>Test your agent</button></section><section className="metric-grid"><article className="metric-card"><div className="metric-label"><span>Total calls</span><PhoneCall size={17}/></div><strong>{data.calls.total}</strong><p><b>{data.calls.completed}</b> completed</p></article><article className="metric-card"><div className="metric-label"><span>Leads captured</span><UsersRound size={17}/></div><strong>{data.leads.total}</strong><p><b>{data.leads.new}</b> need attention</p></article><article className="metric-card"><div className="metric-label"><span>Resolved by AI</span><Sparkles size={17}/></div><strong>{resolvedRate}%</strong><p><b>{resolved}</b> without callback</p></article><article className="metric-card"><div className="metric-label"><span>After-hours</span><CalendarDays size={17}/></div><strong>{data.calls.after_hours}</strong><p><b>Calls answered</b> while closed</p></article></section><section className="insight-grid"><article className="panel performance-panel"><div className="panel-heading"><div><h2>Call activity</h2><p>Answered calls over the last 7 days</p></div><button>This week <ChevronDown size={14}/></button></div><div className="chart-wrap"><ResponsiveContainer width="100%" height="100%"><BarChart data={activity} margin={{ top:12, right:8, left:-25, bottom:0 }}><CartesianGrid stroke="#edf0f4" vertical={false}/><XAxis dataKey="day" axisLine={false} tickLine={false}/><YAxis allowDecimals={false} axisLine={false} tickLine={false}/><Tooltip/><Bar dataKey="calls" fill="#6b63f6" radius={[6,6,2,2]} maxBarSize={36}/></BarChart></ResponsiveContainer></div></article><article className="panel outcomes-panel"><div className="panel-heading"><div><h2>Call outcomes</h2><p>Where conversations ended</p></div></div><div className="outcome-visual"><div className="pie-wrap"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={outcomes} dataKey="value" innerRadius={43} outerRadius={62} paddingAngle={3} stroke="none">{outcomes.map(item => <Cell key={item.name} fill={item.color}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer><span><strong>{data.calls.total}</strong><small>total</small></span></div></div><div className="legend"><span><i className="indigo"/>Resolved <b>{resolved}</b></span><span><i className="amber"/>Callback <b>{callbackCount}</b></span><span><i className="rose"/>Escalated <b>{data.calls.escalated}</b></span></div></article></section><section className="panel recent-panel"><div className="panel-heading"><div><h2>Recent calls</h2><p>Latest conversations handled by your agent</p></div><Link href="/calls">View all calls →</Link></div><div className="table-scroll"><table><thead><tr><th>Caller</th><th>Requirement</th><th>Time</th><th>Outcome</th></tr></thead><tbody>{data.recent_calls.map(call => { const status = call.escalated ? "Escalated" : call.lead_id ? "Callback" : "Resolved"; return <tr key={call.id}><td><div className="caller-cell"><span>{(call.caller_name || "U")[0]}</span><div><strong>{call.caller_name || "Unknown caller"}</strong><small>{call.caller_phone || "Number unavailable"}</small></div></div></td><td>{call.lead_requirement || "General enquiry"}</td><td>{new Intl.RelativeTimeFormat("en", { numeric:"auto" }).format(-Math.max(0, Math.round((referenceTime - new Date(call.started_at).getTime()) / 3600000)), "hour")}</td><td><mark className={`status ${call.escalated ? "rose" : call.lead_id ? "amber" : "green"}`}>{status}</mark></td></tr>; })}</tbody></table></div></section></div>;
}
